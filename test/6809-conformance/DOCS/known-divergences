CONF6809 known divergencesTest IDs listed here are disagreements with DOCS/claims.md that have alreadybeen investigated and characterised. The runner reports them as KNOWN ratherthan as new findings, so a familiar defect cannot drown out a fresh one. Arun that changes a known divergence -- a different verdict than the onerecorded in DOCS/expected -- is still flagged, because that is news too.Format: "<test-id> <explanation>".t09 Stock NitrOS-9 loses an update in a concurrent read-modify-write on onet09 record, where the cited manual passage says RBF's own record lockingt09 must serialize the two processes and prevent exactly that. Root-causedt09 to clobbered B/X registers in the NitrOS-9 RBF path, not to the test:t09 see project memory nitros9-rbf-lostupdate-root-cause. This is a realt09 defect in the clone, and the reason the suite exists.