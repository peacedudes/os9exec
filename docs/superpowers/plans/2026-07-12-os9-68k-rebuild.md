# OS-9 68k Skill Content Rebuild — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fully redo `os9-dev`'s 68k-facing skill content (plus `os9-systems-dev`'s 68k-facing content) via a card-based extraction → sift/merge → repack pipeline, replacing the prior condensation-based content wholesale.

**Architecture:** Sequential Haiku subagents extract atomic, tagged "cards" from each primary source document (13 documents: the existing 68k tier + the Guru book, now included as an ordinary source). Cards accumulate in a git-tracked working directory in this repo (not `/tmp` — the 6809 phase lost ~1400 raw cards to an ephemeral scratchpad; this pipeline must not repeat that). Claude authors CONCEPT/ROSETTA cards from its own accumulated OS-9 experience. A sift/merge pass consolidates the full pile (new 68k cards + already-clean 6809 synthesized docs) by topic, splitting on any real 6809/68k divergence. A final repack writes new skill files in two tiers (crushed eager core, uncrushed reference).

**Tech Stack:** Markdown card files, Claude Code subagent dispatch (Haiku for extraction, Sonnet for authoring/sift/repack), this repo's git history for persistence of intermediate work.

## Global Constraints

- **Never navigate above `os9exec-git_code` directly** (no `ls`/`find`/`cd` into `../os9/txtResources` etc. from the main session) — that tree holds copyrighted source material. Only a *dispatched subagent*, given a specific named document to locate and read, may access it — this mirrors the pattern already used successfully for the 6809 and prior 68k passes.
- **Card template** (fields, in order): `id`, `type` (`FACT|BEHAVIOR|CONCEPT|GOTCHA|ROSETTA`), `target` (`6809|68k|all`), `verify` (`VERIFIED|from-manual|authored`), `topic`, `claim`, `context`, `source`.
- **Never average across targets.** `target: all` only if the fact is identical on 6809 and 68k. Any real divergence becomes sibling cards `<id>-6809` / `<id>-68k`.
- **`FACT` cards may quote official Microware tables verbatim** (cited). Every other card type must be re-expressed in our own words — never paraphrase-in-place.
- **Omit anything a competent LLM already knows generically** (K&R C, generic BASIC, generic Unix). Every card should carry the *delta* — what's OS-9-specific, or where 6809/68k/Unix diverge from expectation.
- **No chapter/section mirroring.** Card piles are flat and unordered; only the final repack imposes topic structure, and that structure must not match any single source document's own table of contents.
- **Cards are Claude's working format, not a human-facing deliverable** — the schema may be amended mid-pipeline if a better shape emerges for a specific batch of facts; don't force-fit at the cost of losing information.
- **6809 is not re-extracted.** Existing files at `~/.claude/skills/os9-dev/references/6809/*.md` and `~/.claude/skills/os9-systems-dev/references/6809-level2-mmu.md` are reused as already-clean input to the sift/merge phase.
- **The Guru book is an ordinary source this pass** — cited like any manual, no special exclusion or flag.

---

## File Structure

**New working directory (git-tracked, this repo):**
```
docs/superpowers/plans/2026-07-12-os9-68k-rebuild/
  extraction-prompt-template.md   # canonical prompt text, filled in per-document
  cards/
    error-codes-pilot.md          # Task 2 pilot output
    <doc-slug>.md                 # one file per source document (Task 3)
    authored-concepts.md          # Task 4 output
    basic09-bnf.md                # Task 5 output
  merged/
    <topic-slug>.md                # Task 6 output, one file per topic cluster
```

**Modified/replaced (outside this repo, in the shared skill directories):**
- `~/.claude/skills/os9-dev/references/common/*.md` (all six files)
- `~/.claude/skills/os9-dev/references/c/*.md` (all three files)
- `~/.claude/skills/os9-dev/references/basic09/os9-68k-basic-cheatsheet.md`, `basic09-vs-68k-differences.md`
- `~/.claude/skills/os9-dev/references/68k/os9-68k-assembly.md`
- `~/.claude/skills/os9-dev/references/INDEX.md`, `SOURCES.md`
- `~/.claude/skills/os9-systems-dev/references/device-drivers.md`, `file-managers.md`, `kernel-internals.md`
- `~/.claude/skills/os9-systems-dev/references/INDEX.md`, `SOURCES.md`

**Not modified:** `~/.claude/skills/os9-dev/references/6809/*`, `~/.claude/skills/os9-dev/references/basic09/basic09-cheatsheet.md` (the 6809 BASIC09 file), `basic09/gotchas.md`, `~/.claude/skills/os9-systems-dev/references/6809-level2-mmu.md`.

---

### Task 1: Canonical extraction prompt template

**Files:**
- Create: `docs/superpowers/plans/2026-07-12-os9-68k-rebuild/extraction-prompt-template.md`

**Interfaces:**
- Produces: the exact prompt text every Task 2/3 dispatch fills in (`{{DOC_NAME}}`, `{{OUTPUT_PATH}}`, `{{DOC_CAVEATS}}`) and hands to a fresh Haiku subagent.

- [ ] **Step 1: Write the template file**

```markdown
# Extraction prompt template

Fill in {{DOC_NAME}}, {{OUTPUT_PATH}}, {{DOC_CAVEATS}} before dispatching.

---

You are extracting facts from one OS-9 reference document for a public,
freely-distributable Claude Code skill. You have no context beyond this
prompt — do not assume anything about prior sessions.

**Your source document:** locate the file matching "{{DOC_NAME}}" under
`/Users/rdoggett/mine/os9/XXX/os9exec/os9/txtResources/68k/` (plain text,
already extracted from PDF via pdftotext -layout — read it directly, do not
re-run any extraction tool). If more than one file could match, read enough
of each to confirm which one is "{{DOC_NAME}}" before proceeding.

**Known caveats for this specific document:** {{DOC_CAVEATS}}

**First step, always:** confirm this document's actual architecture scope
(6809 vs 68000) by reading enough of it to check for register names,
addressing modes, or explicit statements — filenames and even manual titles
have been wrong before (a "68k" folder RMA assembler manual turned out to be
pure 6809). If this document turns out to be 6809-only or otherwise out of
scope for a 68k skill, STOP and report that finding instead of extracting.

**What to produce:** read the entire document and emit a flat, unordered
list of cards, one per atomic fact. Do NOT organize your output by the
document's own chapter or section structure — that structural mirroring is
exactly what caused a prior version of this skill to be rejected as too
close to its source. Shuffle mentally; there is no "right order."

Each card:

```
--- CARD ---
id:        <short-kebab-slug, unique within this file>
type:      FACT | BEHAVIOR | CONCEPT | GOTCHA | ROSETTA
target:    68k | all
verify:    from-manual
topic:     <short cluster tag, e.g. process-mgmt, io, modules, basic09-syntax>
claim:     <the fact, in your own words — 1-4 lines>
context:   <the caveat that keeps this from being misleading if read alone,
            or "-" if none applies>
source:    {{DOC_NAME}} §<page/section locator if the text has one, else a
            short quoted anchor phrase>
--- END ---
```

Rules:
- `type: FACT` may quote an official data table (syscall codes, error
  numbers, byte offsets, register layouts) verbatim inside `claim:` — cite
  it. Every other type must be written in your own words. If you catch
  yourself producing a sentence that's just a shortened version of the
  source's own sentence, rewrite it from the underlying fact instead.
- Emit `CONCEPT` and `GOTCHA` cards too, not just `FACT`/`BEHAVIOR` — many
  reference manuals state mental-model points directly ("the module is the
  fundamental unit of...", "unlike file systems that..."). Capture those.
- Do NOT emit a card for anything a competent programmer already knows
  generically (standard C syntax, standard Unix concepts, standard BASIC
  keywords). Only OS-9-specific facts, or places where OS-9 diverges from
  what a Unix/Linux-experienced or generic-BASIC-experienced person would
  expect, are worth a card. When in doubt, ask: "would a Claude model
  trained broadly already know this?" — if yes, skip it.
- Set `target: 68k` for anything specific to the 68000 architecture or this
  manual's specific claims; set `target: all` only if you have independent
  reason to believe the fact holds on 6809 too (rare — you have not read
  any 6809 material, so default to `target: 68k` unless the document
  itself states the fact is architecture-independent).
- No fixed count — extract everything worth a card, however many that is.

**Output:** write the full card list to {{OUTPUT_PATH}}. Do not summarize or
truncate. Report back only a one-paragraph summary of what you found and any
architecture-scope surprises.
```

- [ ] **Step 2: Verify the template covers every Global Constraint**

Read the template back against the Global Constraints list above. Confirm
it explicitly states: never-average/never-mirror-chapters/verbatim-tables-
only-for-FACT/omit-generic-knowledge/emit-all-five-types. If any is
missing, add it before proceeding — this template is reused by every
remaining extraction task, so a gap here propagates to all of them.

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/plans/2026-07-12-os9-68k-rebuild/extraction-prompt-template.md
git commit -m "Docs: add canonical card-extraction prompt template for 68k rebuild"
```

---

### Task 2: Pilot extraction — error code table

**Files:**
- Create: `docs/superpowers/plans/2026-07-12-os9-68k-rebuild/cards/error-codes-pilot.md`
- Read: `~/.claude/skills/os9-dev/references/common/error-codes.md` (existing table, for cross-check only — do not copy from it, it may itself carry old wording)

**Interfaces:**
- Consumes: the template from Task 1.
- Produces: a verified `FACT`-card group covering the OS-9 error code table, used later by Task 6's merge and Task 7's repack of `common/error-codes.md`.

This is the explicit pilot from the design spec — small, well-bounded, and independently checkable against a cross-check already on record (the existing table's codes were already confirmed correct once via a second independent source).

- [ ] **Step 1: Dispatch extraction against the primary source**

Fill in the Task 1 template with `{{DOC_NAME}} = "OS-9 v2.4 Technical
Reference Manual"`, `{{OUTPUT_PATH}} = docs/superpowers/plans/2026-07-12-os9-68k-rebuild/cards/error-codes-pilot.md`,
`{{DOC_CAVEATS}} = "Focus only on the error code appendix/table for this
pilot — ignore the rest of the document, a separate task covers it in
full."` Dispatch as a fresh Haiku subagent (general-purpose type, no prior
context).

- [ ] **Step 2: Dispatch a second, independent cross-check extraction**

Same template, `{{DOC_NAME}} = "the independent 1985-era OS-9/68000
technical manual"` (per `SOURCES.md`, this was the manual used to
cross-check `common/error-codes.md` originally), same output file appended
(not overwritten) — instruct the agent to also note where its error-code
list matches or diverges from any pre-existing content already in the
output file it's appending to.

- [ ] **Step 3: Verify the pilot output**

Read `cards/error-codes-pilot.md`. Confirm: every card follows the exact
template fields; codes match the existing `common/error-codes.md` table
(spot-check at least 10 codes); the known genuine divergence (68k's
`E$BPNum` reused for both 200 and 201, vs. 6809's distinct `E$PthFul` at
200) appears as a `context:` note, not silently dropped or "fixed."

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/plans/2026-07-12-os9-68k-rebuild/cards/error-codes-pilot.md
git commit -m "Docs: pilot error-code card extraction for 68k rebuild"
```

If this pilot reveals a problem with the template (agent restated the
source too closely, missed the divergence note, produced chapter-ordered
output), fix `extraction-prompt-template.md` now, before Task 3 runs it
another 12 times.

---

### Task 3: Full extraction pass — remaining source documents

**Files:**
- Create: one file per document under `docs/superpowers/plans/2026-07-12-os9-68k-rebuild/cards/`

**Interfaces:**
- Consumes: the template from Task 1 (as amended after Task 2's pilot).
- Produces: the full raw card pile, consumed by Task 6 (sift/merge).

Source documents (same tier already triaged for the prior 68k pass, per
`~/.claude/skills/os9-dev/SOURCES.md`, plus the Guru book added this time):

| # | `{{DOC_NAME}}` | Output file | `{{DOC_CAVEATS}}` |
|---|---|---|---|
| 1 | OS-9 v2.4 Technical Reference Manual (remainder, excluding the error-code appendix already done in Task 2) | `cards/v24-trm.md` | Skip the error-code appendix — already extracted in Task 2. |
| 2 | OS-9 C Compiler manual | `cards/c-compiler-manual.md` | A prior pass found 6809-only "direct page" content bled into this manual's description of storage classes — confirm architecture scope per-fact, not just for the whole document. |
| 3 | Technical I/O Manual | `cards/technical-io-manual.md` | - |
| 4 | BASIC09 Reference Manual (Rev H) | `cards/basic09-rev-h.md` | This is architecture-independent (BASIC09 runs on both 6809 and 68k) — most cards should be `target: all` unless the manual calls out a 68k-specific numeric limit. |
| 5 | Using Professional OS-9 (v2.4) | `cards/using-professional-os9.md` | - |
| 6 | OS-9 Insights (edition 3) | `cards/os9-insights.md` | Richest known source for IPC (pipes/signals/events/semaphores) — don't under-extract that section. |
| 7 | The OS-9 Primer | `cards/os9-primer.md` | - |
| 8 | the independent 1985-era OS-9/68000 technical manual (already used in Task 2 for error codes) | `cards/independent-1985-manual.md` | Skip the error-code section — already done in Task 2. Richest known source for F$DFork/F$DExec and scheduler detail. |
| 9 | the mislabeled "RBasic" manual (a third BASIC09 manual) | `cards/rbasic-manual.md` | Confirm this is actually BASIC09 content (as found previously) before extracting — if it turns out to be true RBasic-specific content, report that instead. |
| 10 | Disk File Organization manual | `cards/disk-file-organization.md` | - |
| 11 | The OS-9 Guru book | `cards/guru-book.md` | Included as an ordinary source this pass. This document previously produced near-verbatim card text when agents were told to "preserve examples" — do NOT ask this agent to preserve any example; describe what an example demonstrates only. |

**Do not include** `OS9_Relocating_Macro_Assembler_Manual.txt` — confirmed
6809-only despite living in the `68k/` folder (see `SOURCES.md`'s
"Known-bad source" section).

- [ ] **Step 1: Dispatch extraction for document #1, verify output, commit**

Fill in the template per the table row, dispatch a fresh Haiku subagent,
read the resulting card file, confirm it follows the template and contains
no chapter-mirroring or verbatim-non-table prose, then:

```bash
git add docs/superpowers/plans/2026-07-12-os9-68k-rebuild/cards/v24-trm.md
git commit -m "Docs: extract cards from OS-9 v2.4 TRM for 68k rebuild"
```

- [ ] **Step 2: Repeat Step 1 for documents #2 through #11**

Same procedure — dispatch, read and verify, commit — once per remaining row
in the table above, in order, sequentially (not parallel; parallel
dispatch was tried during the 6809 phase and found slower in practice).
Use each row's own `{{DOC_CAVEATS}}` verbatim. Commit message pattern:
`Docs: extract cards from <manual name> for 68k rebuild`.

- [ ] **Step 3: Final verification of the full pile**

Grep every file under `cards/` for signs of chapter-title mirroring (a card
`topic:` sequence that matches a table of contents) and for suspiciously
long `claim:` fields that might be near-verbatim prose. Spot-check three
cards per file against their cited source page/section. Fix any file that
fails this check by re-dispatching that one document's extraction with a
sharper `{{DOC_CAVEATS}}` note about what went wrong.

---

### Task 4: Authored CONCEPT/ROSETTA cards

**Files:**
- Create: `docs/superpowers/plans/2026-07-12-os9-68k-rebuild/cards/authored-concepts.md`

**Interfaces:**
- Consumes: nothing from other tasks (written directly from this project's own accumulated hands-on OS-9 experience).
- Produces: `CONCEPT`/`ROSETTA` cards folded into Task 6's merge alongside extracted ones.

This is Claude's own contribution, not delegated to a subagent — it draws
on this project's live `os9exec`/REPL history (documented across this
project's own memory: syscall behaviors confirmed by compiling and running
real programs, `chx`/`chd` resolution rules confirmed against both live
testing and the manual, the module-header/`int`-size errors caught via
`sizeof()` on the real toolchain, etc.).

- [ ] **Step 1: Write cards directly**

Using the same template (`type: CONCEPT` or `ROSETTA`, `verify: authored`,
`source: authored`), write one card per genuinely OS-9-specific mental
model point or Linux/Unix-mapping this project has directly confirmed —
covering at minimum: the module-as-fundamental-unit concept, `fork`/`exec`
vs. `F$Fork`'s single-call model, path resolution by open-mode
(execute vs. data) rather than by command, and the shell control-key
inversions (`^A`/`^W`) already documented in `unix-differences.md`. Write
as many as the accumulated experience actually supports — no fixed count,
and skip anything already well-covered by an extracted card from Task 3.

- [ ] **Step 2: Cross-check against existing common/unix-differences.md**

Read the current `~/.claude/skills/os9-dev/references/common/unix-differences.md`
and confirm every Tier-1 point already documented there has a corresponding
authored card here (carrying its content forward, not losing it in the
rebuild) — add any that are missing.

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/plans/2026-07-12-os9-68k-rebuild/cards/authored-concepts.md
git commit -m "Docs: author CONCEPT/ROSETTA cards from project OS-9 experience"
```

---

### Task 5: BASIC09 BNF-style grammar cards

**Files:**
- Create: `docs/superpowers/plans/2026-07-12-os9-68k-rebuild/cards/basic09-bnf.md`

**Interfaces:**
- Consumes: `cards/basic09-rev-h.md` (Task 3) and `cards/rbasic-manual.md` (Task 3) for source facts.
- Produces: grammar cards folded into Task 6/7's BASIC09 cheatsheet repack.

- [ ] **Step 1: Write one informal-grammar card per syntax area**

Five areas, matching the design spec: declarations/types, control-flow
constructs (block terminators are the real delta), procedure/parameter
structure, `PRINT`/`PRINT USING` formatting, `TYPE`/record definitions.
Each card: informal shorthand grammar (not strict BNF — skip obvious leaf
productions like `<digit>`), paired with one tiny valid BASIC09 example,
and a `context:` line stating specifically how this differs from what a
model would expect from generic BASIC (e.g. `PROCEDURE`/`ENDPROC`
block structure vs. an interpreter with line-number-only structure).

- [ ] **Step 2: Commit**

```bash
git add docs/superpowers/plans/2026-07-12-os9-68k-rebuild/cards/basic09-bnf.md
git commit -m "Docs: add BASIC09 grammar-shortcut cards for 68k rebuild"
```

---

### Task 6: Sift and merge

**Files:**
- Create: `docs/superpowers/plans/2026-07-12-os9-68k-rebuild/merged/<topic-slug>.md` (one per topic cluster — expect roughly: `process-mgmt.md`, `io.md`, `modules.md`, `memory.md`, `ipc.md`, `errors.md`, `tools-and-shell.md`, `c-language.md`, `basic09-language.md`, `assembly.md`, `mental-model-and-rosetta.md`)
- Read: every file under `cards/` (Tasks 2–5) plus the existing 6809 synthesized docs (`~/.claude/skills/os9-dev/references/6809/*.md`, `~/.claude/skills/os9-systems-dev/references/6809-level2-mmu.md`)

**Interfaces:**
- Consumes: the full raw card pile.
- Produces: topic-clustered, deduplicated card files, the direct input to Task 7's repack.

- [ ] **Step 1: Cluster by topic**

Read every card across every file (new 68k cards + Task 4/5 authored cards
+ existing 6809 docs treated as a source of additional cards you extract
from them now, since they're already clean). Group into the topic clusters
listed above (add or rename clusters if the actual pile doesn't fit them
well — this grouping is not fixed in advance).

- [ ] **Step 2: Merge only true duplicates**

Within each topic cluster, merge two cards into one only if their `claim:`
is genuinely identical in substance across `target: 68k` and a 6809
equivalent — set the merged card's `target: all`. If they differ in any
real way, keep them as separate `<id>-6809` / `<id>-68k` cards in the same
cluster file, adjacent to each other so the divergence is visible.

- [ ] **Step 3: Prune ruthlessly**

Delete any card that, on a second read, turns out to restate something a
competent LLM already knows, or duplicates another card in the same
cluster without adding a distinct fact. This step is explicitly a "with a
vengeance" cut per the user's own framing — err toward deleting, not
keeping "just in case."

- [ ] **Step 4: Verify no cross-target averaging slipped through**

Grep every merged file for `target: all` and manually confirm each one
actually holds on both 6809 and 68k (cross-check against a 6809 source
file if any doubt exists) — this is the single failure mode Opus's design
review flagged as most likely to slip through unnoticed.

- [ ] **Step 5: Commit**

```bash
git add docs/superpowers/plans/2026-07-12-os9-68k-rebuild/merged/
git commit -m "Docs: sift and merge 68k+6809 card pile by topic for rebuild"
```

---

### Task 7: Repack into skill files

**Files:**
- Modify (full rewrite): all files listed in "Modified/replaced" under File Structure above

**Interfaces:**
- Consumes: `merged/*.md` from Task 6.
- Produces: the actual shipped skill content.

- [ ] **Step 1: Write the eager core**

`SKILL.md` (both skills) plus a compact mental-model/Rosetta core drawn from
`merged/mental-model-and-rosetta.md` — crushed hard, delta-only, no filler,
CONCEPT/ROSETTA cards leading, GOTCHA cards close behind.

- [ ] **Step 2: Write the reference tier**

One target file per remaining merged topic cluster, following the existing
directory convention (`common/`, `c/`, `basic09/`, `68k/` in `os9-dev`;
flat `references/` in `os9-systems-dev`). Reference tables (error codes,
syscall parameters, module offsets) copied in full from the `FACT` cards —
not crushed, not thinned. BASIC09 file leads with the Task 5 grammar cards
plus one worked example, matching the shape already established for the
6809 BASIC09 file.

- [ ] **Step 3: Verify nothing reads as chapter-mirrored**

For each new file, confirm its section order matches the topic clustering
from Task 6, not any single source document's table of contents.

- [ ] **Step 4: Commit**

```bash
git -C ~/.claude/skills add -A
git -C ~/.claude/skills commit -m "Rebuild 68k skill content via card pipeline" 2>&1 || true
```

(If `~/.claude/skills` isn't a git repo, skip the commit — just confirm the
files are written; note this to the user since these directories are the
actual public-release artifact.)

---

### Task 8: Provenance and cross-reference sweep

**Files:**
- Modify: `~/.claude/skills/os9-dev/SOURCES.md`, `~/.claude/skills/os9-dev/references/INDEX.md`
- Modify: `~/.claude/skills/os9-systems-dev/SOURCES.md`, `~/.claude/skills/os9-systems-dev/references/INDEX.md`

- [ ] **Step 1: Update SOURCES.md provenance table**

Add a row per repacked file naming which primary manuals (including the
Guru book, now listed like any other) fed into it, per Task 6's merge
output.

- [ ] **Step 2: Update INDEX.md routing**

Confirm every file still referenced routes correctly; remove any entry for
a file deleted in Task 7; add entries for any new file.

- [ ] **Step 3: Sweep for dangling cross-references**

Grep both skills' `references/` trees for any `[[...]]`-style or filename
reference to a file that no longer exists post-rebuild; fix each one.

- [ ] **Step 4: Commit**

```bash
git -C ~/.claude/skills add -A
git -C ~/.claude/skills commit -m "Update provenance and cross-references after 68k rebuild" 2>&1 || true
```

---

### Task 9: Verification pass

- [ ] **Step 1: Paraphrase spot-check**

Pick five repacked cards/facts at random per source manual, open the
source at the cited locator, and confirm the final wording could not be
produced by mechanically shortening the source's own sentence.

- [ ] **Step 2: Live-verify upgradeable BEHAVIOR claims**

For any `BEHAVIOR` card whose claim can be checked against the running 68k
`os9exec`/REPL (per `tools/os9repl.sh`), do so and upgrade its `verify` tag
before it's folded into prose (the prose itself doesn't carry the tag, but
this determines whether a claim can be stated with confidence).

- [ ] **Step 3: Report to the user**

Summarize: card counts per type/topic, number of genuine 6809/68k
divergences found (the actual payoff this pipeline is meant to prove out,
per [[feedback_card_taxonomy_skepticism]] — lead with this, not with
process mechanics), and any open follow-ups (CD-i section, the
still-unresolved "who carries OS-9 forward" shout-out).

---

## Self-Review Notes

- **Spec coverage:** Task 1–2 cover the card template + pilot; Task 3 covers
  full extraction incl. Guru book inclusion; Task 4 covers Claude-authored
  CONCEPT/ROSETTA; Task 5 covers BASIC09-only BNF; Task 6 covers sift/merge
  with the never-average rule; Task 7 covers the two-tier repack; Task 8
  covers provenance/INDEX; Task 9 covers verification. All spec sections
  have a task.
- **Placeholder scan:** the extraction prompt template, card format, and
  document table all contain real, complete content — no TBD/TODO.
- **Type consistency:** the five-field-plus-three (`id/type/target/verify/
  topic/claim/context/source`) card schema is used identically in every
  task; the `{{DOC_NAME}}/{{OUTPUT_PATH}}/{{DOC_CAVEATS}}` template
  variables are used identically in Tasks 2 and 3.
