# OS9exec project rules

The `Source/OS9exec_core/os9defs/` directory contains only symlinks to the
`*_from_book.h` headers in the project root.  The proprietary Microware source
files that were previously here have been removed from the working directory.
No access restrictions apply.
